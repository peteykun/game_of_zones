<h1>List of Problems</h1>
<hr>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th width="5%">#</th>
      <th width="50%">Problem Name</th>
      <th colspan="3">Controls</th>
    </tr>
  </thead>

  <tbody>
    <% @problems.each do |problem| %>
      <tr>
        <td><%= problem.id %></td>
        <td><%= problem.name %></td>
        <td>
          <%= link_to problem do %>
          <span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span> Read Statement
          <% end %>
        </td>
        <td>
          <%= link_to request_input_problem_path(problem) do %>
          <span class="glyphicon glyphicon-download" aria-hidden="true"></span> Request input
          <% end %>
        </td>
        <td>
          <%= link_to submit_output_problem_path(problem) do %>
          <span class="glyphicon glyphicon-upload" aria-hidden="true"></span> Submit output
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
